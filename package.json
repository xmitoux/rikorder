{
  "name": "rikorder",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "render:deploy": "pnpm install --frozen-lockfile && pnpm -F db build && pnpm -F backend build",
    "deploy:vercel": "pnpm install --frozen-lockfile && pnpm -F db build && pnpm -F frontend build"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2",
    "turbo": "^2.0.4"
  },
  "packageManager": "pnpm@9.4.0"
}
